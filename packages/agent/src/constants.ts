/**
 * Shared constants for the agent package.
 */

/**
 * Probability threshold for triggering random failures (10%).
 * Used by both DELAY and HTTP_GET_JSON executors.
 */
export const RANDOM_FAILURE_PROBABILITY = 0.1;
